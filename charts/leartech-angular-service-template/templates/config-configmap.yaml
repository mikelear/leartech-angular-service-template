apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "leartech-angular-service-template.fullname" . }}-config
  labels:
    {{- include "leartech-angular-service-template.labels" . | nindent 4 }}
data:
  api.conf.json: |
    {{- toJson .Values.config | nindent 4 }}
